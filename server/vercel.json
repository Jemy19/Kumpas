{
    "version": 2,
    "installCommand": "npm install --force --legacy-peer-deps",
    "builds": [
      { "src": "index.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.js" }
    ]
  }